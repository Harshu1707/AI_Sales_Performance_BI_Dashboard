# Global Sales Performance & Business Insights Dashboard

An end-to-end Business Analyst portfolio project built around the Kaggle Sample/Global Superstore dataset. The project analyzes retail order data to uncover revenue, profit, customer, regional, product, discount, and growth insights, then converts those findings into a dashboard-ready BI solution.

Dataset source: [Kaggle search - Sample Superstore Dataset](https://www.kaggle.com/search?q=sample+superstore+dataset)

## Project Overview

This project simulates a real business intelligence engagement for a retail superstore. The workflow starts with raw Excel/CSV data, cleans and validates the dataset, performs SQL and Python exploratory analysis, defines dashboard KPIs, and documents executive-level business recommendations.

Primary business questions:

- Which regions, states, categories, and products drive the most sales and profit?
- Where is the business losing money?
- How do discounts affect profitability?
- Which customer segments contribute the most value?
- What sales pattern should leadership expect over the next 3 months?

## Repository Structure

```text
.
|-- data/
|   |-- raw/                 # Place Kaggle CSV/XLS/XLSX files here
|   |-- processed/           # Cleaned outputs generated by scripts
|   `-- README.md
|-- dashboard/
|   |-- powerbi_dashboard_build_guide.md
|   |-- tableau_dashboard_build_guide.md
|   `-- screenshots/
|       `-- README.md
|-- dax/
|   `-- powerbi_measures.dax
|-- docs/
|   |-- business_insights_report.md
|   |-- dashboard_blueprint.md
|   |-- project_workflow.md
|   `-- resume_ready_project_description.md
|-- scripts/
|   |-- clean_superstore.py
|   |-- eda_superstore.py
|   `-- forecast_sales.py
|-- sql/
|   `-- superstore_analysis.sql
|-- requirements.txt
`-- README.md
```

## Dataset Information

Use a Kaggle Superstore dataset that contains order-level retail transactions. The project supports both common Sample Superstore and Global Superstore variants.

Expected columns include:

- Order ID, Order Date, Ship Date
- Customer ID, Customer Name, Segment
- Region, Country, State, City
- Category, Sub-Category, Product Name
- Sales, Quantity, Discount, Profit, Shipping Cost

If your downloaded file does not include `Shipping Cost`, the cleaning script creates the field with `0.00` so dashboard and SQL logic remain stable.

## Tools Used

- Power BI or Tableau for dashboarding
- SQL for analytical querying
- Excel/CSV as the source data format
- Python for cleaning, EDA, and forecasting
- DAX for KPI measures in Power BI

## KPI Definitions

- Total Sales: Sum of sales revenue.
- Total Profit: Sum of order-line profit.
- Profit Margin %: Total Profit divided by Total Sales.
- Sales Growth %: Period-over-period sales growth.
- Average Order Value: Total Sales divided by distinct orders.
- Customer Count: Distinct customer count.
- Total Quantity Sold: Sum of units sold.

## Dashboard Pages

1. Executive Overview
   - KPI cards
   - Monthly sales trend
   - Sales forecast
   - Regional sales and profit comparison

2. Regional Performance
   - Region and state map
   - State-level profit ranking
   - Sales, profit, and margin by geography

3. Product & Category Analysis
   - Category revenue share
   - Top 10 products
   - Loss-making products
   - Sub-category profitability

4. Customer & Discount Insights
   - Segment analysis
   - Customer lifetime value
   - Profit vs discount scatter plot
   - Discount band profitability

## Steps To Run

1. Download a Sample Superstore or Global Superstore dataset from Kaggle.
2. Place the file in `data/raw/`.
3. Install Python dependencies:

```bash
pip install -r requirements.txt
```

4. Clean the data:

```bash
python scripts/clean_superstore.py --input data/raw/YOUR_FILE.csv --output data/processed/superstore_clean.csv
```

For Excel:

```bash
python scripts/clean_superstore.py --input "data/raw/Sample - Superstore.xls" --output data/processed/superstore_clean.csv
```

5. Run EDA:

```bash
python scripts/eda_superstore.py --input data/processed/superstore_clean.csv --output-dir data/processed
```

6. Generate a 3-month forecast:

```bash
python scripts/forecast_sales.py --input data/processed/superstore_clean.csv --output data/processed/sales_forecast_3_months.csv
```

7. Import `data/processed/superstore_clean.csv` and `data/processed/sales_forecast_3_months.csv` into Power BI or Tableau.
8. Use `dax/powerbi_measures.dax` for Power BI KPI measures.
9. Use `sql/superstore_analysis.sql` for SQL portfolio analysis.

## SQL Explanations

The SQL file includes queries for:

- Top 5 best-selling products
- Monthly revenue analysis
- Region-wise profit analysis
- Highest profit categories
- Customer lifetime value
- Most discounted products
- Sales growth trends
- Loss-making products

The queries assume a cleaned table named `superstore_orders` with snake_case columns, matching the output from the Python cleaning script.

## Business Insights

The final insight report is available in `docs/business_insights_report.md`. It includes a reusable executive summary format plus SQL/Python-driven insight prompts so results can be finalized after the dataset is loaded.

Typical expected findings from Superstore analysis include:

- High sales regions are not always the highest-margin regions.
- Heavy discounting often produces negative profit in specific sub-categories.
- Technology and office supply categories commonly produce strong revenue, but profitability varies by product.
- A small set of customers and products usually account for a disproportionate share of revenue.

## Dashboard Screenshots

Add final Power BI/Tableau screenshots to `dashboard/screenshots/`.

Recommended screenshots:

- `01_executive_overview.png`
- `02_regional_performance.png`
- `03_product_analysis.png`
- `04_customer_discount_insights.png`

## Future Improvements

- Add row-level security by region.
- Add automated dataset refresh.
- Add profit anomaly alerts.
- Compare actual sales against forecast.
- Build a customer retention cohort analysis.
- Add a what-if discount simulator in Power BI.

# InsightIQ - AI Sales Intelligence Dashboard

InsightIQ is a modern full-stack AI business intelligence dashboard for the Kaggle Sample Superstore dataset. It combines a React + Vite executive UI, Flask analytics APIs, Scikit-learn forecasting, CSV upload support, and Gemini `gemini-2.5-flash` insights/chat.

## Features

- Premium dark SaaS dashboard with glassmorphism cards, gradient lighting, responsive layout, hover states, Framer Motion animations, and animated KPI counters.
- KPI cards for total sales, total profit, profit margin, average order value, customer count, total orders, and sales growth.
- Recharts visualizations for monthly sales trend, region sales, category profit mix, top products, discount impact, segment analysis, state heatmap, and 3-month forecast.
- Gemini-powered executive insights and AI business chatbot with local analytics fallbacks when no API key is configured.
- Flask REST APIs for KPI calculations, chart payloads, forecasting, AI insights, chatbot responses, and CSV upload.
- SQLite-ready backend structure with a seed utility.
- Dashboard screenshot export, product search, upload action, refresh action, and theme toggle control.

## Project Structure

```text
backend/
|-- app.py
|-- requirements.txt
|-- routes/
|-- services/
|-- models/
|-- utils/
|-- data/
|-- .env
frontend/
|-- src/
|   |-- services/
|   |-- App.jsx
|   |-- main.jsx
|   |-- styles.css
|-- package.json
|-- tailwind.config.js
|-- vite.config.js
data/
|-- raw/superstore.csv
|-- processed/
```

## Backend Setup

```bash
cd backend
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

The Flask API runs at `http://127.0.0.1:5000`.

## Gemini Setup

Add your Gemini API key to `backend/.env`:

```env
GEMINI_API_KEY=your_api_key_here
GEMINI_MODEL=gemini-2.5-flash
```

If `GEMINI_API_KEY` is empty, the dashboard still works using deterministic local analytics summaries.

## Frontend Setup

```bash
cd frontend
npm install
npm run dev
```

The React app runs at `http://127.0.0.1:5173` and proxies `/api` requests to Flask.

## API Endpoints

- `GET /api/kpis` - KPI summary.
- `GET /api/charts` - chart-ready analytics data.
- `GET /api/forecast` - next 3 months sales forecast.
- `GET /api/insights` - Gemini or local executive insights.
- `POST /api/chat` - AI chatbot answer from dataset context.
- `POST /api/upload` - upload a replacement CSV file.

## Dataset

The app uses the existing Kaggle Sample Superstore file at `data/raw/superstore.csv`. You can also upload a compatible CSV through the dashboard upload button.

## Deployment Notes

- Deploy Flask with Gunicorn or a WSGI host and set `GEMINI_API_KEY` as an environment variable.
- Build the frontend with `npm run build` and host the generated `frontend/dist` on Vercel, Netlify, or a static server.
- Configure the frontend host to route `/api/*` to the Flask service.

## Future Improvements

- Add JWT login/register and protected dashboard routes.
- Persist uploaded datasets and chat sessions per authenticated user.
- Add Prophet as an optional forecasting engine.
- Add PDF report generation and scheduled email summaries.
- Add deployment manifests for Docker, Render, Railway, or Azure.

---
